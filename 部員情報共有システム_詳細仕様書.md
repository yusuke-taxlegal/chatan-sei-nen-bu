# 北谷町商工会青年部 部員情報共有システム 詳細仕様書

> 最終更新日: 2026年3月17日

## 1. システム概要

**北谷町商工会青年部** の部員同士の交流とビジネスマッチングを促進するための情報共有Webアプリケーション。

- **公開URL**: https://chatan-members-site.web.app
- **技術スタック**: Vue.js 3 + Vite 5 + Firebase（Authentication / Firestore / Storage / Hosting）+ Firebase Cloud Functions
- **AIアシスタント**: プロフィール作成支援 GPTs（ChatGPT）を提供
  - https://chatgpt.com/g/g-686e38248dd08191ab49db04c0ac3c8d-qing-nian-bu-hurohuiruzuo-cheng-kun?model=gpt-4o

---

## 2. ユーザーフロー全体像

```
① ユーザー登録・ログイン（メール、Google認証）
    ↓
② アカウント作成（Google初回ログイン時自動、またはメールで新規作成）
    ↓
③ マイページで詳細プロフィール・SNS情報を入力＆LINEアカウント連携
    ↓
④ 部員一覧に反映（現役・卒部生タブ、業種や同級生フィルター機能あり）
    ↓
⑤ 他の部員が詳細プロフィールを閲覧＆各種SNS・YouTubeへアクセス
```

---

## 3. ページ構成と各ページの役割

| パス               | ページ名             | ログイン必要 | 説明                                                                     |
| ------------------ | -------------------- | :----------: | ------------------------------------------------------------------------ |
| `/`                | トップページ         |      ✗       | システム紹介・使い方ガイド・GPTsリンク・QRコード                         |
| `/about`           | 商工会青年部宣言     |      ✗       | 商工会青年部宣言・誓いの言葉・青年部の歌および音源再生                   |
| `/login`           | ログイン・登録       |      ✗       | メール、Google、あるいは連携済みLINEアカウントでのログインおよび登録     |
| `/forgot-password` | パスワード再設定     |      ✗       | パスワードリセットメール送信フォーム（同一画面で結果を表示）             |
| `/mypage`          | マイページ           |      ✓       | **プロフィール編集・アカウント連携（メイン入力画面）**                   |
| `/members`         | 部員一覧             |      ✗       | 全部員のプロフィール検索・一覧（バッジ表示・フィルタ機能）               |
| `/member/:id`      | 部員詳細             |      ✗       | 個別部員の詳細表示・NotebookLMプロンプト生成                             |
| `/terms`           | 利用規約             |      ✗       | サービスの利用規約（8条）                                                |
| `/privacy`         | プライバシーポリシー |      ✗       | 個人情報保護方針（6条）                                                  |

> **注意**: `/add-sample-data` パスも存在しますが、開発・デモ用途専用のツールページです。

---

## 4. アカウント作成・ログイン（`/login`）

システムへのエントリーポイントであり、以下の3つの認証方式をサポートしています。

### 4.1 メールアドレス・パスワードでの登録/ログイン

- フォームからメールアドレスとパスワード（6文字以上）を入力してログイン（登録）を行う。
- 「アカウント作成」と「ログイン」をトグルで切り替え可能。
- パスワードリセット機能あり（`/forgot-password` へ遷移）。

### 4.2 Googleアカウントでのログイン

- Googleポップアップ認証でログイン（`signInWithPopup`）。
- 初回ログイン時は自動でFirestoreに初期プロフィール（名前・画像URL）が作成される。
- Googleアカウントユーザーのメール・パスワード変更はGoogle側に依存。

### 4.3 LINEアカウントでのログイン（LIFF）

- マイページで事前にLINE連携を完了したユーザーに限り、ログイン画面に「LINEログイン」ボタンが表示される（`localStorage` の `hasLinkedLINE` フラグで制御）。
- LINE内ブラウザ（LIFF）で自動アクセス可能。
- LINEアクセストークンをCloud Functions（`createLineCustomToken`）で検証し、Firebaseカスタムトークンを発行してサインイン。

### 4.4 パスワード再設定（`/forgot-password`）

- メールアドレス入力後、Firebaseの `sendPasswordResetEmail` でリセットメールを送信。
- 同一画面内で成功・エラーメッセージをインライン表示。
- エラーケース:
  - `auth/user-not-found`: 未登録のメールアドレス
  - `auth/invalid-email`: 無効なメールアドレス形式
  - その他エラー: 汎用エラーメッセージ

---

## 5. プロフィール入力項目（`/mypage`）★ 最重要セクション

マイページでは、部員が以下の情報を入力・編集します。これが **部員一覧** と **部員詳細ページ** に表示されます。
※入力された生年月日から「同級生」「卒部」「Last Year」などのステータスが自動計算されます。

### 5.1 プロフィール入力フォーム

| 項目名             | 入力タイプ               | データベースフィールド                                   | アプリでの主な用途・備考                                                              |
| ------------------ | ------------------------ | -------------------------------------------------------- | ------------------------------------------------------------------------------------- |
| プロフィール画像   | ファイル選択 → トリミング | `profileImageUrl`                                        | アップロード時に円形にトリミング（1:1）。JPEG形式でStorageへ保存。                    |
| 氏名               | `text`                   | `name`                                                   | 部員の本名                                                                            |
| フリガナ           | `text`                   | `phoneticName`                                           | カタカナ表記                                                                          |
| 会社名             | `text`                   | `company`                                                | 所属企業・屋号                                                                        |
| 業種               | `select`相当（テキスト） | `industry`                                               | 部員一覧のフィルター等に利用                                                          |
| 趣味・関心         | `text`（カンマ区切り等） | `hobbies`                                                | タグ風に表示される趣味・関心事                                                        |
| 自己紹介・事業内容 | `textarea`               | `bio`                                                    | 自由記述。事業内容や自己紹介など                                                      |
| ニーズ対応         | `textarea`               | `needs`                                                  | 自分が対応可能な需要の説明                                                            |
| 青年部内取引事例   | `textarea`               | `pastTransactions`                                       | 部員同士の過去の取引実績                                                              |
| 提供できる情報     | `textarea`               | `providableInfo`                                         | 共有可能な知識・ノウハウ等                                                            |
| 求めている情報     | `textarea`               | `seekingInfo`                                            | 探している情報やパートナー                                                            |
| ウェブサイト       | `url`                    | `website`                                                | コーポレートサイトやブログ表示用                                                      |
| SNS各種リンク      | 各`url`                  | `twitter`, `facebook`, `instagram`, `tiktok`, `sns`      | 個別に分かれたSNSリンク。対応アイコンで一覧や詳細に表示される                         |
| YouTubeリンク      | `url`                    | `youtube`                                                | YouTube動画URLの場合は詳細画面で埋め込み動画プレイヤーとして表示、チャンネルはリンク |
| 現役職             | `text`                   | `currentRole`                                            | 該当する場合は「🏅 現 〇〇」バッジとして表示される                                    |
| 過去の役職         | `array`（複数テキスト）  | `pastRoles`                                              | 「📜 〇〇経験」バッジとして表示される                                                 |
| 入部年度           | `number`                 | `enrollmentYear`                                         | 「📅 〇〇年入部」バッジとして表示される                                               |
| 生年月日           | `date`（`YYYY-MM-DD`）   | `birthDate`                                              | 画面には出ないが「🎓 卒部」「🔥 Last Year」「🤝 同級生」の内部計算に利用される         |

### 5.2 プロフィール画像のトリミング機能

- 画像選択後、**円形トリミングモーダル** が表示される（`vue-advanced-cropper` 使用）。
- トリミング後 JPEG 形式に変換。
- Firebase Storage（`profileImages/{userId}/{filename}`）に保存。

### 5.3 マイページの表示モード

マイページには「**閲覧モード**」と「**編集モード**」の2つのモードがある。

- **閲覧モード**: 現在のプロフィールを表示。QRコードによるデジタル名刺機能も提供。
- **編集モード**: 全フィールドの入力・更新が可能。

---

## 6. アカウント管理の入力項目（`/mypage` 下部）

- **メールアドレスの変更**: 現在のパスワード確認の上で変更可（メール/パスワード認証ユーザーのみ）。
- **パスワード変更**: 既存パスワード確認後、新しいパスワード（6文字以上）へ変更（メール/パスワード認証ユーザーのみ）。
- **Googleアカウント連携**: メールアドレス/LINEログインユーザーがGoogleアカウントを後から紐付け可能。
- **LINEアカウント連携**: LINEアカウント（LIFF経由）を紐付け/解除可能（Cloud Functions連携）。
  - 連携時: `lineUid` と `lineLinkedAt`（サーバータイムスタンプ）をFirestoreに保存。
  - 解除時: Firestoreから `lineUid` と `lineLinkedAt` を削除。
- **アカウント削除**: プロフィールデータ（Firestore）、プロフィール画像（Storage）、認証情報（Auth）をすべて完全に削除する。

---

## 7. データベース構造（Firestore）

### 7.1 コレクション: `profiles`

ドキュメントID = Firebase Auth のユーザーUID

```json
{
  "name": "田中 太郎",
  "phoneticName": "タナカ タロウ",
  "company": "田中建設株式会社",
  "industry": "建設・建築業",
  "hobbies": "ゴルフ, 釣り",
  "bio": "自己紹介テキスト...",
  "needs": "対応可能なニーズ...",
  "pastTransactions": "過去の青年部内取引...",
  "providableInfo": "提供できる情報...",
  "seekingInfo": "求めている情報...",
  "website": "https://example.com",
  "sns": "https://その他SNS.com",
  "twitter": "https://twitter.com/tanaka",
  "facebook": "https://facebook.com/tanaka",
  "instagram": "https://instagram.com/tanaka",
  "tiktok": "https://tiktok.com/@tanaka",
  "youtube": "https://youtube.com/watch?v=...",
  "profileImageUrl": "https://firebasestorage...",
  "currentRole": "副部長",
  "pastRoles": ["委員長", "理事"],
  "enrollmentYear": 2015,
  "birthDate": "1985-05-15",
  "lineUid": "U1234567890abcdef...",      // LINE連携済の場合のみ
  "lineLinkedAt": "Timestamp"             // LINE連携日時（サーバータイムスタンプ）
}
```

### 7.2 Firestoreセキュリティルール

```
/profiles/{userId}
  - read:             誰でも可（認証不要）
  - create/update/delete: 本人のみ（request.auth.uid == userId）
```

プロフィール情報は全ユーザーに公開ですが、書き込みは本人のみ可能です。

---

## 8. 部員一覧ページでの表示項目（`/members`）

リスト上で各部員の簡易情報がカード形式で表示されます。

- **タブ切り替え機能**: 「現役部員」と「OB・OG（卒部生）」をタブで切り替え。
- **フィルター・検索**:
  - 「業種」プルダウンによる絞り込み機能
  - ログインユーザー向けの「🤝 同級生表示」トグルスイッチ
- **表示内容**:
  - プロフィール画像、氏名、フリガナ、会社名（業種付き）
  - 各種バッジ：同級生、卒部、Last Year、現役職、役職経験、入部年度
  - 事業内容プレビュー（3行までに省略表示）
  - SNSアイコンボタン群（X、FB、IG、TikTok、YouTube、その他）
- **表示順序**: アクセスごとにランダム順（Fisher-Yates シャッフルアルゴリズム）。
- **NotebookLM 案内バナー**: 一覧下部に表示。

### 8.1 フィルタリングロジック（`processedMembers` / `filteredMembers`）

1. **タブフィルター**: `getGraduationStatus()` の `isGraduated` フラグで現役/卒部を判定。
2. **業種フィルター**: `member.industry` との完全一致で絞り込み。
3. **同級生フィルター**: 自分の `birthDate` から `getSchoolYear()` で学年を算出し、同じ学年の部員を抽出。自分自身は同級生バッジを表示しないが、フィルター時には自分も表示される。

---

## 9. 部員詳細ページでの表示項目（`/member/:id`）

部員の全詳細情報と、高度な連携サポート機能を提供します。

- **各種プロフィール項目**: 会社名、事業内容、趣味・関心、ニーズ、過去の取引、提供・求めている情報。
- **バッジとステータス**:
  - ログイン中のユーザーの情報（自身の生年月日）と照らし合わせ、「🤝 同級生」バッジを自動表示。
  - プロフィールの生年月日に基づき「🎓 卒部」または「🔥 Last Year」バッジを表示。
- **YouTube連動**: 設定されたURLが動画ページの場合は、正規表現でビデオIDを抽出して専用の埋め込みプレイヤーを表示。チャンネルページの場合はリンクとして表示。
- **SNS連携**: 個別に設定された各SNSリンクをアイコンボタンとして表示。
- **NotebookLM プロンプト自動生成機能** (`NotebookLMPromptGenerator` コンポーネント):
  - 詳細ページ下部の `<details>` 折りたたみパネルで提供。
  - A-1: マーケティング知識ソース（ハイパワーマーケティング要約）のコピー
  - A-2: 表示中メンバーのプロフィールをNotebookLMソース用テキストに整形してコピー
  - B-1: 事業分析・戦略依頼プロンプトの生成
  - B-2: ビジネスマッチング提案プロンプトの生成（相手と自分のプロフィールを掛け合わせ）
  - 応用編: スライド資料・音声解説・インフォグラフィック用プロンプト
  - アイソメ・イエロー デザインシステム: 等角投影イラスト×エレクトリックイエローのスライドデザイン指定プロンプト
  - NotebookLM活用ロードマップガイド（業種別活用法含む）

---

## 10. 卒部・学年ステータスの計算ロジック（`src/utils/memberUtils.js`）

### 10.1 `getGraduationStatus(birthDateString)`

日本の年度（4月〜翌3月）基準で卒部・Last Year を判定します。

| 条件                                      | ステータス         |
| ----------------------------------------- | ------------------ |
| 46歳になる年度の4月1日以降（現在日時）    | `isGraduated: true`（🎓 卒部） |
| 45歳になる年度の4月1日〜46歳の年度4月1日 | `isLastYear: true`（🔥 Last Year） |
| それ以外                                  | 現役               |

- 1〜3月生まれは前年度扱い（`birthFiscalYear = birthYear - 1`）。
- 入力値が空・不正の場合は `{ isGraduated: false, isLastYear: false }` を返す。

### 10.2 `getSchoolYear(birthDateString)`

日本の学校年度（4月〜翌年3月）基準で学年（年度）を算出します。

- 4月以降生まれ → `birthYear` をそのまま学年年度として返す。
- 1〜3月生まれ → `birthYear - 1` を学年年度として返す。
- 同値の場合は同級生と判定される。

---

## 11. サンプルデータ一覧（開発・デモ用）

`/add-sample-data` ページから10名分のサンプルデータをFirestoreに一括追加できます。

| ID     | 氏名       | 会社名                   | 業種                 |
| ------ | ---------- | ------------------------ | -------------------- |
| user1  | 田中 太郎  | 田中建設株式会社         | 建設・建築業         |
| user2  | 佐藤 花子  | Cafe Sunset              | 宿泊・飲食サービス業 |
| user3  | 山田 次郎  | 山田ITソリューションズ   | 情報通信業（IT）     |
| user4  | 鈴木 美咲  | 鈴木美容室               | 生活関連サービス等   |
| user5  | 高橋 大輔  | タカハシ水産             | 水産業               |
| user6  | 伊藤 かな子| いとう会計事務所         | 士業・専門サービス   |
| user7  | 渡辺 健一  | ワタナベ自動車整備工場   | 自動車整備業         |
| user8  | （略）     | ...                      | ...                  |
| user9  | （略）     | ...                      | ...                  |
| user10 | （略）     | ...                      | ...                  |

---

## 12. 認証方式

| 認証方式                  | 新規登録           | ログイン | アカウント連携・解除 |
| ------------------------- | :----------------: | :------: | :------------------: |
| メールアドレス/パスワード |         ✓          |    ✓     |          -           |
| Googleアカウント          |      自動登録      |    ✓     |   マイページから可   |
| LINEアカウント（LIFF）    | マイページから連携 |    ✓     |   マイページから可   |

- Googleやメールで登録したユーザーが、マイページのアカウント管理画面からLINE連携を行うことで、以降LINEログインが可能になります。
- Googleアカウントで登録したユーザーのメール・パスワード変更はプロバイダ側に依存します。

---

## 13. Firebase Cloud Functions（`functions/index.js`）

| 関数名                    | タイプ                      | 役割                                                                                                          |
| ------------------------- | --------------------------- | ------------------------------------------------------------------------------------------------------------- |
| `createLineCustomToken`   | `onRequest`（公開・CORS対応） | LINEアクセストークンを検証→Firebaseカスタムトークンを発行（LINEログイン）。既存ユーザー紐付けにも対応。    |
| `linkLineAccount`         | `onCall`（認証必須）        | ログイン済みユーザーのFirestoreプロフィールに `lineUid` と `lineLinkedAt` を保存（LINE連携）。重複チェックあり。 |
| `unlinkLineAccount`       | `onCall`（認証必須）        | Firestoreから `lineUid` と `lineLinkedAt` フィールドを削除（LINE連携解除）。                                   |

### 13.1 `createLineCustomToken` の処理フロー

1. LINEアクセストークンをリクエストボディから受け取る。
2. LINE API（`/oauth2/v2.1/verify`）でトークンの有効期限を検証。
3. LINE API（`/v2/profile`）でユーザープロフィール（UID・表示名・画像）を取得。
4. Firestore の `profiles` コレクションで `lineUid` を検索し、既存ユーザーへの紐付けを確認。
5. Firebase カスタムトークンを発行して返す。

---

## 14. Firebase設定情報

```
プロジェクトID:     chatan-members-site
Auth Domain:        chatan-members-site.firebaseapp.com
Storage Bucket:     chatan-members-site.firebasestorage.app
Messaging Sender ID: 1024502175390
App ID:             1:1024502175390:web:0f79130f64007cff0e4d8f
Measurement ID:     G-QDDSG76947
```

---

## 15. アプリケーション構造（ファイル一覧）

```
chatan-sei-nen-bu/
├── index.html
├── package.json
├── firebase.json
├── firestore.rules
├── .env
├── functions/
│   └── index.js                         # Cloud Functions (LINE連携)
├── src/
│   ├── main.js                          # Vueアプリ起動エントリーポイント
│   ├── App.vue                          # ルートコンポーネント（ヘッダー/フッター/PWA通知）
│   ├── firebase.js                      # Firebase初期化（auth / db / storage をエクスポート）
│   ├── router/
│   │   └── index.js                     # ルーティング定義・ルートガード
│   ├── composables/
│   │   └── useAuth.js                   # 認証状態グローバル管理（モジュールレベルのリアクティブ変数）
│   ├── utils/
│   │   └── memberUtils.js               # 卒部・学年ステータス計算ユーティリティ
│   ├── components/
│   │   └── NotebookLMPromptGenerator.vue # NotebookLMプロンプト生成コンポーネント
│   ├── views/
│   │   ├── HomeView.vue                 # トップページ
│   │   ├── AboutView.vue                # 商工会青年部宣言・青年部の歌
│   │   ├── LoginView.vue                # ログイン・新規登録
│   │   ├── MyPageView.vue               # マイページ（最大ファイル・約2000行）
│   │   ├── MemberListView.vue           # 部員一覧
│   │   ├── MemberDetailView.vue         # 部員詳細
│   │   ├── ForgotPasswordView.vue       # パスワード再設定（現行使用）
│   │   ├── PasswordResetView.vue        # パスワード再設定（旧実装・ルーター未登録）
│   │   ├── PasswordResetSentView.vue    # パスワード再設定完了（旧実装・ルーター未登録）
│   │   ├── AddSampleDataView.vue        # サンプルデータ投入ツール（開発用）
│   │   ├── TermsView.vue                # 利用規約
│   │   └── PrivacyView.vue              # プライバシーポリシー
│   └── assets/
│       ├── main.css
│       └── base.css
└── dist/                                # ビルド済みファイル（Firebase Hostingデプロイ対象）
```

---

## 16. ルーティング詳細（`src/router/index.js`）

| パス               | ルート名        | コンポーネント          | 遅延読み込み | 認証ガード   |
| ------------------ | --------------- | ----------------------- | :----------: | :----------: |
| `/`                | home            | HomeView                |      ✗       |      ✗       |
| `/about`           | about           | AboutView               |      ✓       |      ✗       |
| `/login`           | login           | LoginView               |      ✓       |      ✗       |
| `/mypage`          | mypage          | MyPageView              |      ✓       |      ✓       |
| `/members`         | members         | MemberListView          |      ✓       |      ✗       |
| `/member/:id`      | member-detail   | MemberDetailView        |      ✓       |      ✗       |
| `/add-sample-data` | add-sample-data | AddSampleDataView       |      ✓       |      ✗       |
| `/forgot-password` | forgot-password | ForgotPasswordView      |      ✓       |      ✗       |
| `/terms`           | terms           | TermsView               |      ✓       |      ✗       |
| `/privacy`         | privacy         | PrivacyView             |      ✓       |      ✗       |

- **ルートガード**: `meta: { requiresAuth: true }` が設定されたルートは `onAuthStateChanged` でログイン状態を確認し、未ログインの場合は `/login` へリダイレクト。
- **スクロール制御**: ページ遷移時は常にトップへスムーズスクロール。ブラウザの戻る/進むボタン使用時は前回のスクロール位置を復元。

---

## 17. 主要コンポーザブル（`src/composables/useAuth.js`）

```javascript
// モジュールレベルでリアクティブ変数を管理（アプリ全体で共有）
const user = ref(auth.currentUser)
onAuthStateChanged(auth, (u) => { user.value = u })

// 外部からは readonly として公開
const useAuth = () => ({ user: readonly(user) })
```

- **モジュールスコープ**: `user` は モジュール初期化時に1度だけ生成され、全コンポーネントで共有。
- **読み取り専用**: `readonly()` でラップしているため、コンポーネントから直接書き換えは不可。

---

## 18. トップページの主要コンテンツ（`HomeView.vue`）

1. **ヒーローセクション**: 「ビジネスネットワーキングの新しいかたち」メッセージ + フローティングカード（ネットワーキング・ビジネス連携・地域貢献）
2. **課題提起**: 顔と名前の不一致、事業内容を知る機会の不足、連携きっかけの不在
3. **システム目的**: 交流活性化、ビジネスマッチング促進、青年部の魅力向上
4. **使い方ガイド**: 3ステップ（登録→一覧閲覧→詳細確認）
5. **AIアシスタント案内**: GPTsによるプロフィール作成支援へのリンク
6. **QRコード**: スマートフォンからのアクセス用QRコード表示（`qrcode.vue` 使用）
- ログイン状態に応じてヒーローボタンが切り替わる（未ログイン時: 「今すぐ参加」「ログイン」、ログイン時: 「部員一覧へ」のみ）。

---

## 19. UI/UX特徴

- **モダンUI**: ガラスモーフィズム（Glasscard）、アニメーション（AOS、`@vueuse/motion`）
- **レスポンシブデザイン**: モバイル・タブレット・デスクトップ対応
- **PWA対応**: `vite-plugin-pwa` でサービスワーカー生成。アプリ更新時にトースト通知を表示（`App.vue`）。
- **LINE統合（LIFF）**: LINEアプリ内ブラウザ（LIFF）によるシームレスなログイン
- **AI連携**: ChatGPT（GPTs）やNotebookLMを利用した情報活用をアプリの導線として組み込み
- **スムーズなナビゲーション**: デスクトップ用ナビ + ハンバーガーメニュー（`App.vue` で管理）

---

## 20. 技術スタック詳細

### 20.1 フロントエンド依存関係（`package.json`）

| パッケージ               | バージョン  | 用途                           |
| ------------------------ | ----------- | ------------------------------ |
| `vue`                    | ^3.4.29     | UIフレームワーク（Composition API） |
| `vue-router`             | ^4.3.3      | SPAルーティング                |
| `firebase`               | ^10.12.2    | Auth / Firestore / Storage     |
| `firebase-admin`         | ^13.6.1     | Cloud Functions用管理SDK       |
| `@line/liff`             | ^2.27.3     | LINEログイン・LIFF SDK         |
| `vue-advanced-cropper`   | ^2.8.9      | プロフィール画像トリミング     |
| `qrcode.vue`             | ^3.8.0      | デジタル名刺QRコード生成       |
| `@vueuse/motion`         | ^3.0.3      | アニメーション                 |
| `aos`                    | ^2.3.4      | スクロールアニメーション       |
| `@headlessui/vue`        | ^1.7.23     | アクセシブルなUIコンポーネント |
| `@heroicons/vue`         | ^2.2.0      | アイコンライブラリ             |
| `vue3-lottie`            | ^3.3.1      | Lottieアニメーション           |
| `lottie-web`             | ^5.13.0     | Lottie実行エンジン             |
| `vite-plugin-pwa`        | ^1.2.0      | PWA対応（サービスワーカー）    |
| `dotenv`                 | ^17.3.1     | 環境変数管理                   |
| `vite`                   | ^5.3.1      | ビルドツール                   |

### 20.2 ビルド・開発スクリプト

| コマンド              | 内容                      |
| --------------------- | ------------------------- |
| `npm run dev`         | 開発サーバー起動          |
| `npm run build`       | 本番ビルド（`dist/`）     |
| `npm run preview`     | ビルド結果のプレビュー    |
| `npm run lint`        | ESLint（自動修正付き）    |
| `npm run format`      | Prettier フォーマット     |
| `npm run add-sample-data` | サンプルデータ投入スクリプト |

---

## 21. 今後の拡張予定

- 詳細な検索・キーワードフィルタリング機能
- お気に入り機能
- メッセージング機能
- イベント管理・出欠機能
- 青年部内のタイムライン・通知機能
